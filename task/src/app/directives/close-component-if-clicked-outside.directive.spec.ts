import { CloseComponentIfClickedOutsideDirective } from './close-component-if-clicked-outside.directive';

describe('CloseComponentIfClickedOutsideDirective', () => {
  it('should create an instance', () => {
    const directive = new CloseComponentIfClickedOutsideDirective();
    expect(directive).toBeTruthy();
  });
});
