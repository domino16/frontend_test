import { AlphabeticSortPipe } from './alphabetic-sort.pipe';

describe('AlphabeticSortPipe', () => {
  it('create an instance', () => {
    const pipe = new AlphabeticSortPipe();
    expect(pipe).toBeTruthy();
  });
});
